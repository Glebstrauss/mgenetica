required:
  - project_id
  - raw_dir
  - processed_dir
  - analysis_dir
  - outputs_dir
  - work_dir

recommended:
  - metadata_dir
  - reports_dir
  - lifecycle_policy

lifecycle_policy:
  raw_dir: immutable
  processed_dir: generated
  analysis_dir: heavy_outputs
  outputs_dir: consolidated_products
  work_dir: local_only
