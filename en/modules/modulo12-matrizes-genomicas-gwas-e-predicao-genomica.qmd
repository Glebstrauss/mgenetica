---
title: "Module 12 — Genomic Matrices, GWAS and Genomic Prediction"
description: "Construction of the G matrix, genome-wide association study (GWAS) and genomic prediction by GBLUP."
lang: en
---

```{=html}
<div class="module-header">
  <span class="module-badge">MODULE 12 · GENOMICS</span>
  <div class="module-meta"><span>⏱ ~50 min</span><span>Level: Advanced</span></div>
</div>
<div class="module-objectives">
  <h3>What will you learn</h3>
  <ul>
    <li>Construct the genomic relationship matrix G (VanRaden, 2008)</li>
    <li>Run a simplified GWAS and interpret the Manhattan plot</li>
    <li>Understanding GBLUP and why it outperforms pedigree BLUP</li>
    <li>Evaluate the accuracy of genomic prediction in a validation set</li>
  </ul>
</div>
<div class="module-orientation" aria-label="How to study this module">
  <span>Reading</span>
  <span>Simulation in R</span>
  <span>Interpretation</span>
</div>
```

::: {.module-reading-rhythm role="note" aria-label="Recommended reading pace for the module"}
**Reading pace**

Read first to understand the question, run the script to generate evidence and only then move on to the exercise and quiz.
:::

::: {.module-session-plan role="region" aria-label="Short module session plan"}
::: {.module-session-plan-copy}
**Short session plan**

Use this module in a closed session: enter with a question, produce scripted evidence, and leave with a short technical decision.
:::

::: {.module-session-plan-grid role="list" aria-label="Module study session steps"}
::: {.module-session-plan-item role="listitem"}
**Question**

Define what needs to be clearer before reading.
:::

::: {.module-session-plan-item role="listitem"}
**Evidence**

Rotate or revise the main section in R.
:::

::: {.module-session-plan-item role="listitem"}
**Decision**

Write a technical consequence before leaving.
:::
:::
:::

::: {.module-technical-scan role="region" aria-label="How to read the module's formulas, code and tables"}
::: {.module-technical-scan-copy}
**Read the technical part in three passages**

Don't try to solve formula, code and table at the same time. First locate the concept, then run or follow the script, and finally interpret the output as evidence.
:::

::: {.module-technical-scan-grid role="list" aria-label="Module technical reading passages"}
::: {.module-technical-scan-item role="listitem"}
**Formula**

Identify which terms change the decision.
:::

::: {.module-technical-scan-item role="listitem"}
**Code**

See which parameter was simulated or compared.
:::

::: {.module-technical-scan-item role="listitem"}
**Table**

Read the difference that supports the interpretation.
:::
:::
:::

## From matrix A to matrix G: expected versus realized relatedness

In BLUP with pedigree (Module 08), two half-siblings have a_ij = 0.25 — by definition. But the actual genomic relatedness between them could be 0.18 or 0.31, depending on which segments were inherited. The **matrix G** captures this real variation:

$$\mathbf{G} = \frac{\mathbf{Z}\mathbf{Z}'}{2\sum_j p_j(1-p_j)}$$

Where **Z** is the centered genotype matrix (Z_ij = G_ij − 2p_j).

::: {.callout-note}
**Central concept:** The diagonal of G measures "self-relatedness" — equivalent to 1 + F_i in matrix A, but calculated directly from molecular data. Values ​​> 1.0 indicate greater homozygosity than the population average (endogamy).
:::

```{=html}
<div class="mg-viz" data-mg-viz="gwas"></div>
```

## Complete simulation

::: {.module-script-lab role="region" aria-label="Module 12 script lab"}
::: {.module-script-lab-copy}
**Script lab**

Use the script to link three pieces of evidence: genomic relatedness by G matrix, association signal by GWAS, and prediction by GBLUP. The page shows the main excerpts; the full script allows you to repeat the entire flow.
:::

::: {.module-script-lab-grid role="list" aria-label="How to reproduce Module 12 script"}
::: {.module-script-lab-item role="listitem"}
**R script**

[Open `modulo12.R`](../../scripts/modulo12.R){.entry-link aria-label="Open the full R script for Module 12"}
:::

::: {.module-script-lab-item role="listitem"}
**Generated output**

[View simulated CSV](../../data/modulo12_simulado.csv){.entry-link aria-label="Open the simulated CSV generated by Module 12"}
:::

::: {.module-script-lab-item role="listitem"}
**What to change**

Scale `n_qtl` and `n_anim` in separate scenarios to compare genetic architecture and reference population size.
:::

::: {.module-script-lab-item role="listitem"}
**What to interpret**

Explain when the decision depends more on the G matrix, a GWAS peak or the genomic prediction accuracy.
:::
:::
:::

```r
#| label: simula-genomica
set.seed(112)
n_anim <- 200; n_snp <- 300

p_alelo <- rbeta(n_snp,2,2)
p_alelo <- pmax(0.05, pmin(0.95, p_alelo))
Z_raw   <- matrix(NA, n_anim, n_snp)
for (j in 1:n_snp) {
  a1 <- rbinom(n_anim,1,p_alelo[j]); a2 <- rbinom(n_anim,1,p_alelo[j])
  Z_raw[,j] <- a1+a2
}
cat(sprintf("Dados: %d animais × %d SNPs\n", n_anim, n_snp))
```

```r
#| label: constroi-G
# VanRaden (2008) Method 1
p2    <- colMeans(Z_raw)/2
Z     <- sweep(Z_raw, 2, 2*p2, "-")   # centralização: Z_ij = G_ij - 2p_j
denom <- 2*sum(p2*(1-p2))
G     <- (Z %*% t(Z)) / denom

cat("=== Matriz G (5×5 primeiras entradas) ===\n")
print(round(G[1:5,1:5], 4))
cat(sprintf("\nDiagonal G: média=%.4f  min=%.4f  max=%.4f\n",
            mean(diag(G)), min(diag(G)), max(diag(G))))
cat(sprintf("Off-diagonal: média=%.4f  DP=%.4f\n",
            mean(G[upper.tri(G)]), sd(G[upper.tri(G)])))
```

```r
#| label: simula-fenotipos-qtl
# Simula fenótipos com 10 QTLs de efeito moderado
n_qtl  <- 10
qtl_id <- sample(1:n_snp, n_qtl)
betas  <- rnorm(n_qtl, 0, sqrt(20))
a_gen  <- Z_raw[,qtl_id] %*% betas
e_obs  <- rnorm(n_anim, 0, sqrt(var(a_gen)*1.5))
y      <- 300 + a_gen + e_obs

h2_real <- var(a_gen)/var(y)
cat(sprintf("\nh² simulado: %.3f  QTLs em SNPs: %s\n",
            h2_real, paste(qtl_id[1:5], collapse=",")))
```

```r
#| label: gwas
# GWAS: regressão do fenótipo em cada SNP individualmente
y_std <- scale(y)[,1]   # padroniza
pvals <- numeric(n_snp); betas_gwas <- numeric(n_snp)
for (j in 1:n_snp) {
  x <- Z_raw[,j]
  if (var(x)<1e-6) { pvals[j]<-1; next }
  fit <- lm(y_std~x)
  betas_gwas[j] <- coef(fit)[2]
  pvals[j]      <- summary(fit)$coefficients[2,4]
}

gwas_res <- data.frame(snp=1:n_snp, beta=round(betas_gwas,4), logp=-log10(pvals))
cat("=== Top 5 SNPs no GWAS ===\n")
print(head(gwas_res[order(-gwas_res$logp),], 5))
cat(sprintf("\nQTLs verdadeiros (primeiros 5): %s\n", paste(sort(qtl_id)[1:5], collapse=",")))
cat(sprintf("QTLs detectados (p<0.05/n):     %d de %d\n",
            sum(pvals[qtl_id] < 0.05/n_snp), n_qtl))
```

```r
#| fig-height: 3.5
#| label: manhattan-plot
plot(gwas_res$snp, gwas_res$logp, pch=20, col="#4a9fd1",
     xlab="SNP", ylab="-log10(p-valor)",
     main="Manhattan plot — GWAS simplificado",
     ylim=c(0, max(gwas_res$logp)+1))
abline(h=-log10(0.05/n_snp), col="#dc2626", lty=2, lwd=1.5)
points(qtl_id, gwas_res$logp[qtl_id], pch=20, col="#dc2626", cex=1.2)
legend("topright", c("Todos os SNPs","SNPs QTL verdadeiros","Limiar Bonferroni"),
       pch=c(20,20,NA), lty=c(NA,NA,2), col=c("#4a9fd1","#dc2626","#dc2626"), bty="n", cex=0.85)
```

```r
#| label: gblup
# GBLUP: predição com validação cruzada simples
train_id <- 1:(n_anim/2); val_id <- (n_anim/2+1):n_anim
h2_est   <- 0.40; lam <- (1-h2_est)/h2_est

G_train <- G[train_id, train_id]
y_train <- y[train_id]
y_val   <- y[val_id]

# Solução: û = G_train(G_train + λI)⁻¹ × (y_train - mean(y_train))
sol_u   <- solve(G_train + diag(length(train_id))*lam) %*% (y_train - mean(y_train))
G_cross <- G[val_id, train_id]
u_pred  <- mean(y_train) + G_cross %*% sol_u

r_val   <- cor(u_pred, y_val)
cat(sprintf("=== Acurácia GBLUP na validação ===\n"))
cat(sprintf("r(predito, observado) = %.4f\n", r_val))
cat(sprintf("R² = %.4f\n", r_val^2))
cat(sprintf("n treinamento=%d  n validação=%d\n", length(train_id), length(val_id)))
```

## Interpretation

- The Manhattan plot shows clear peaks at the true QTLs (red). With n=200 and 300 SNPs, not all QTLs are detected — demonstrating the power problem in GWAS with small samples.
- The GBLUP accuracy of ~0.5–0.6 with n_training=100 is reasonable. In practice, genomic selection programs use reference populations of 10,000–100,000 animals.
- The diagonal of G with a mean ~1 and variation between 0.8 and 1.3 reveals homozygosity heterogeneity — animals with a high diagonal are more inbred.

::: {.callout-important}
**Interpretation:** GWAS identifies associated regions, but not necessarily the causal gene. The SNP with the highest -log10(p) may be in LD with the true QTL, without being the QTL itself. Functional confirmation requires other approaches.
:::

::: {.module-evidence-path role="note" aria-label="Module evidence path"}
**Path of evidence**

Before the exercise, connect three points: which question the module opened, which script output supports the answer and which technical decision is clearer.
:::

::: {.module-practice-contract role="note" aria-label="Module practice contract"}
**Minimal evidence for this module**

Before completing the track, explain in one sentence when to use G-matrix, when to use GWAS, and when to use genomic prediction to support a selection decision.
:::

## Proposed exercise

1. Increase `n_qtl` to 50 (more polygenic architecture). Does GWAS still detect clear peaks?
2. Compare the diagonal of G with the diagonal of A constructed by the pedigree (Module 09). Are the magnitudes similar?
3. Increase `n_anim` to 500. Does GBLUP accuracy improve significantly?

::: {.module-study-checkpoint role="note" aria-label="Study checkpoint before the quiz"}
**Before the quiz**

Review the biological question, run the main section in R, and write a sentence linking the result to the selection decision. Then proceed to the quiz with that phrase in mind.
:::

::: {.module-takeaways role="note" aria-label="Module takeaways"}
**What to take away from this module**

- Genomic arrays, GWAS, and prediction answer different questions about relatedness, association, and future value.
- The decision applied depends on linking statistical signal, data quality and selection objective.
:::

::: {.quiz-container data-module="12"}
:::

::: {.module-after-quiz role="note" aria-label="Next step after the quiz"}
**After the quiz**

If the quiz result is consistent, review the recorded technical phrases and move on to the certificate. If in doubt, return to the proposed exercise before completing.
:::

::: {.module-completion-note role="region" aria-label="MGenética path closure"}
**Trail closure**

After this module, review the interpretation you wrote in the previous checkpoints. The certificate is the formal closure, but the evidence of learning is the ability to explain when to use pedigree, genomic array, GWAS, or prediction for a selection decision.
:::

::: {.module-close-check role="region" aria-label="Module closing check"}
::: {.module-close-check-copy}
**Before changing pages**

Close the module when the reading, code and quiz support the same conclusion. If any point is left open, go back through the index or search before moving forward.
:::

::: {.module-close-check-grid role="list" aria-label="Module closing criteria"}
::: {.module-close-check-item role="listitem"}
**Reading**

The central question was answered in your own words.
:::

::: {.module-close-check-item role="listitem"}
**Code**

The main output was interpreted, not just executed.
:::

::: {.module-close-check-item role="listitem"}
**Quiz**

The result confirms readiness to move forward or revise.
:::
:::
:::

::: {.module-return-note role="note" aria-label="How to return to the flow after this module"}
**Come back with a decision**

Before changing pages, record whether this module led to advancement, revision or consultation. Use this decision to choose the next click in the index.
:::

```{=html}
<nav class="module-nav" aria-label="Navigation between modules">
  <a class="module-nav-card module-nav-index" href="index.html">
    <span>Index</span>
    <strong>All modules</strong>
  </a>
  <a class="module-nav-card module-nav-prev" href="modulo11-controle-de-qualidade-de-dados-genomicos.html">
    <span>Previous · Module 11</span>
    <strong>Genomic Quality Control</strong>
  </a>
  <a class="module-nav-card module-nav-next" href="../certificate.html">
    <span>Conclusion</span>
    <strong>Generate certificate</strong>
  </a>
</nav>
```
