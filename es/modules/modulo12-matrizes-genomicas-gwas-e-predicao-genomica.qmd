---
title: "Módulo 12: Matrices genómicas, GWAS y predicción genómica"
description: "Construcción de la matriz G, estudio de asociación de todo el genoma (GWAS) y predicción genómica por GBLUP."
lang: es
---

```{=html}
<div class="module-header">
  <span class="module-badge">MÓDULO 12 · GENÓMICA</span>
  <div class="module-meta"><span>⏱ ~50 minutos</span><span>Nivel: Avanzado</span></div>
</div>
<div class="module-objectives">
  <h3>¿Qué aprenderás?</h3>
  <ul>
    <li>Construir la matriz de relación genómica G (VanRaden, 2008)</li>
    <li>Ejecute un GWAS simplificado e interprete el gráfico de Manhattan</li>
    <li>Comprender GBLUP y por qué supera al BLUP de pedigrí</li>
    <li>Evaluar la precisión de la predicción genómica en un conjunto de validación</li>
  </ul>
</div>
<div class="module-orientation" aria-label="Cómo estudiar este módulo">
  <span>Lectura</span>
  <span>Simulación en R</span>
  <span>Interpretación</span>
</div>
```

::: {.module-reading-rhythm role="note" aria-label="Ritmo recomendado de lectura del módulo"}
**Ritmo de lectura**

Lea primero para comprender la pregunta, ejecute el guión para generar evidencia y solo luego continúe con el ejercicio y la prueba.
:::

::: {.module-session-plan role="region" aria-label="Plan corto de sesión del módulo"}
::: {.module-session-plan-copy}
**Plan de sesión corta**

Utilice este módulo en una sesión cerrada: ingrese con una pregunta, presente evidencia escrita y salga con una breve decisión técnica.
:::

::: {.module-session-plan-grid role="list" aria-label="Etapas de la sesión de estudio del módulo"}
::: {.module-session-plan-item role="listitem"}
**Pregunta**

Defina lo que necesita quedar más claro antes de leer.
:::

::: {.module-session-plan-item role="listitem"}
**Evidencia**

Rotar o revisar la sección principal en R.
:::

::: {.module-session-plan-item role="listitem"}
**Decisión**

Escribe una consecuencia técnica antes de partir.
:::
:::
:::

::: {.module-technical-scan role="region" aria-label="Cómo leer fórmulas, código y tablas del módulo"}
::: {.module-technical-scan-copy}
**Lea la parte técnica en tres pasajes**

No intentes resolver fórmulas, códigos y tablas al mismo tiempo. Primero ubique el concepto, luego ejecute o siga el script y finalmente interprete el resultado como evidencia.
:::

::: {.module-technical-scan-grid role="list" aria-label="Pasajes de lectura técnica del módulo"}
::: {.module-technical-scan-item role="listitem"}
**Fórmula**

Identificar qué términos cambian la decisión.
:::

::: {.module-technical-scan-item role="listitem"}
**Código**

Vea qué parámetro fue simulado o comparado.
:::

::: {.module-technical-scan-item role="listitem"}
**Tabla**

Lea la diferencia que sustenta la interpretación.
:::
:::
:::

## De la matriz A a la matriz G: relación esperada versus realizada

En BLUP con pedigrí (Módulo 08), dos medios hermanos tienen a_ij = 0,25, por definición. Pero la relación genómica real entre ellos podría ser de 0,18 o 0,31, dependiendo de qué segmentos se heredaron. La **matriz G** captura esta variación real:

$$\mathbf{G} = \frac{\mathbf{Z}\mathbf{Z}'}{2\sum_j p_j(1-p_j)}$$

Donde **Z** es la matriz de genotipo centrada (Z_ij = G_ij − 2p_j).

::: {.callout-note}
**Concepto central:** La diagonal de G mide la "autorrelación", equivalente a 1 + F_i en la matriz A, pero calculada directamente a partir de datos moleculares. Valores > 1,0 indican mayor homocigosidad que el promedio de la población (endogamia).
:::

```{=html}
<div class="mg-viz" data-mg-viz="gwas"></div>
```

## Simulación completa

::: {.module-script-lab role="region" aria-label="Laboratorio del script del Módulo 12"}
::: {.module-script-lab-copy}
**Laboratorio de guiones**

Utilice el guión para vincular tres pruebas: relación genómica mediante matriz G, señal de asociación mediante GWAS y predicción mediante GBLUP. La página muestra los principales extractos; el script completo le permite repetir todo el flujo.
:::

::: {.module-script-lab-grid role="list" aria-label="Cómo reproducir el script del Módulo 12"}
::: {.module-script-lab-item role="listitem"}
**Script en R**

[Open `modulo12.R`](../../scripts/modulo12.R){.entry-link aria-label="Open the full R script for Module 12"}
:::

::: {.module-script-lab-item role="listitem"}
**Salida generada**

[View simulated CSV](../../data/modulo12_simulado.csv){.entry-link aria-label="Open the simulated CSV generated by Module 12"}
:::

::: {.module-script-lab-item role="listitem"}
**Qué cambiar**

Escale `n_qtl` y `n_anim` en escenarios separados para comparar la arquitectura genética y el tamaño de la población de referencia.
:::

::: {.module-script-lab-item role="listitem"}
**Qué interpretar**

Explique cuándo la decisión depende más de la matriz G, de un pico GWAS o de la precisión de la predicción genómica.
:::
:::
:::

```r
#| label: simula-genomica
set.seed(112)
n_anim <- 200; n_snp <- 300

p_alelo <- rbeta(n_snp,2,2)
p_alelo <- pmax(0.05, pmin(0.95, p_alelo))
Z_raw   <- matrix(NA, n_anim, n_snp)
for (j in 1:n_snp) {
  a1 <- rbinom(n_anim,1,p_alelo[j]); a2 <- rbinom(n_anim,1,p_alelo[j])
  Z_raw[,j] <- a1+a2
}
cat(sprintf("Dados: %d animais × %d SNPs\n", n_anim, n_snp))
```

```r
#| label: constroi-G
# VanRaden (2008) Method 1
p2    <- colMeans(Z_raw)/2
Z     <- sweep(Z_raw, 2, 2*p2, "-")   # centralização: Z_ij = G_ij - 2p_j
denom <- 2*sum(p2*(1-p2))
G     <- (Z %*% t(Z)) / denom

cat("=== Matriz G (5×5 primeiras entradas) ===\n")
print(round(G[1:5,1:5], 4))
cat(sprintf("\nDiagonal G: média=%.4f  min=%.4f  max=%.4f\n",
            mean(diag(G)), min(diag(G)), max(diag(G))))
cat(sprintf("Off-diagonal: média=%.4f  DP=%.4f\n",
            mean(G[upper.tri(G)]), sd(G[upper.tri(G)])))
```

```r
#| label: simula-fenotipos-qtl
# Simula fenótipos com 10 QTLs de efeito moderado
n_qtl  <- 10
qtl_id <- sample(1:n_snp, n_qtl)
betas  <- rnorm(n_qtl, 0, sqrt(20))
a_gen  <- Z_raw[,qtl_id] %*% betas
e_obs  <- rnorm(n_anim, 0, sqrt(var(a_gen)*1.5))
y      <- 300 + a_gen + e_obs

h2_real <- var(a_gen)/var(y)
cat(sprintf("\nh² simulado: %.3f  QTLs em SNPs: %s\n",
            h2_real, paste(qtl_id[1:5], collapse=",")))
```

```r
#| label: gwas
# GWAS: regressão do fenótipo em cada SNP individualmente
y_std <- scale(y)[,1]   # padroniza
pvals <- numeric(n_snp); betas_gwas <- numeric(n_snp)
for (j in 1:n_snp) {
  x <- Z_raw[,j]
  if (var(x)<1e-6) { pvals[j]<-1; next }
  fit <- lm(y_std~x)
  betas_gwas[j] <- coef(fit)[2]
  pvals[j]      <- summary(fit)$coefficients[2,4]
}

gwas_res <- data.frame(snp=1:n_snp, beta=round(betas_gwas,4), logp=-log10(pvals))
cat("=== Top 5 SNPs no GWAS ===\n")
print(head(gwas_res[order(-gwas_res$logp),], 5))
cat(sprintf("\nQTLs verdadeiros (primeiros 5): %s\n", paste(sort(qtl_id)[1:5], collapse=",")))
cat(sprintf("QTLs detectados (p<0.05/n):     %d de %d\n",
            sum(pvals[qtl_id] < 0.05/n_snp), n_qtl))
```

```r
#| fig-height: 3.5
#| label: manhattan-plot
plot(gwas_res$snp, gwas_res$logp, pch=20, col="#4a9fd1",
     xlab="SNP", ylab="-log10(p-valor)",
     main="Manhattan plot — GWAS simplificado",
     ylim=c(0, max(gwas_res$logp)+1))
abline(h=-log10(0.05/n_snp), col="#dc2626", lty=2, lwd=1.5)
points(qtl_id, gwas_res$logp[qtl_id], pch=20, col="#dc2626", cex=1.2)
legend("topright", c("Todos os SNPs","SNPs QTL verdadeiros","Limiar Bonferroni"),
       pch=c(20,20,NA), lty=c(NA,NA,2), col=c("#4a9fd1","#dc2626","#dc2626"), bty="n", cex=0.85)
```

```r
#| label: gblup
# GBLUP: predição com validação cruzada simples
train_id <- 1:(n_anim/2); val_id <- (n_anim/2+1):n_anim
h2_est   <- 0.40; lam <- (1-h2_est)/h2_est

G_train <- G[train_id, train_id]
y_train <- y[train_id]
y_val   <- y[val_id]

# Solução: û = G_train(G_train + λI)⁻¹ × (y_train - mean(y_train))
sol_u   <- solve(G_train + diag(length(train_id))*lam) %*% (y_train - mean(y_train))
G_cross <- G[val_id, train_id]
u_pred  <- mean(y_train) + G_cross %*% sol_u

r_val   <- cor(u_pred, y_val)
cat(sprintf("=== Acurácia GBLUP na validação ===\n"))
cat(sprintf("r(predito, observado) = %.4f\n", r_val))
cat(sprintf("R² = %.4f\n", r_val^2))
cat(sprintf("n treinamento=%d  n validação=%d\n", length(train_id), length(val_id)))
```

## Interpretación

- El gráfico de Manhattan muestra picos claros en los QTL verdaderos (rojo). Con n=200 y 300 SNP, no se detectan todos los QTL, lo que demuestra el problema de energía en GWAS con muestras pequeñas.
- La precisión de GBLUP de ~0,5–0,6 con n_training=100 es razonable. En la práctica, los programas de selección genómica utilizan poblaciones de referencia de entre 10.000 y 100.000 animales.
- La diagonal de G con una media ~1 y una variación entre 0,8 y 1,3 revela heterogeneidad de homocigosidad: los animales con una diagonal alta son más endogámicos.

::: {.callout-important}
**Interpretación:** GWAS identifica regiones asociadas, pero no necesariamente el gen causal. El SNP con el -log10(p) más alto puede estar en LD con el QTL verdadero, sin ser el QTL en sí. La confirmación funcional requiere otros enfoques.
:::

::: {.module-evidence-path role="note" aria-label="Caminho de evidência do módulo"}
**Camino de la evidencia**

Antes del ejercicio, conecte tres puntos: qué pregunta abrió el módulo, qué resultado del guión respalda la respuesta y qué decisión técnica es más clara.
:::

::: {.module-practice-contract role="note" aria-label="Contrato prático do módulo"}
**Evidencia mínima para este módulo**

Antes de completar la pista, explique en una oración cuándo usar G-matrix, cuándo usar GWAS y cuándo usar la predicción genómica para respaldar una decisión de selección.
:::

## Ejercicio propuesto

1. Incrementar `n_qtl` a 50 (arquitectura más poligénica). ¿GWAS todavía detecta picos claros?
2. Comparar la diagonal de G con la diagonal de A construida por el pedigrí (Módulo 09). ¿Las magnitudes son similares?
3. Aumente `n_anim` a 500. ¿Mejora significativamente la precisión de GBLUP?

::: {.module-study-checkpoint role="note" aria-label="Checkpoint de estudo antes do quiz"}
**Antes del cuestionario**

Revise la pregunta biológica, ejecute la sección principal en R y escriba una oración que vincule el resultado con la decisión de selección. Luego pasa al cuestionario con esa frase en mente.
:::

::: {.module-takeaways role="note" aria-label="Resumo técnico do módulo"}
**Qué aprender de este módulo**

- Los conjuntos genómicos, GWAS y la predicción responden a diferentes preguntas sobre la relación, la asociación y el valor futuro.
- La decisión aplicada depende de vincular la señal estadística, la calidad de los datos y el objetivo de selección.
:::

::: {.quiz-container data-module="12"}
:::

::: {.module-after-quiz role="note" aria-label="Próximo passo depois do quiz"}
**Después del cuestionario**

Si el resultado del cuestionario es consistente, revise las frases técnicas grabadas y continúe con el certificado. En caso de duda, regrese al ejercicio propuesto antes de completarlo.
:::

::: {.module-completion-note role="region" aria-label="Cierre del recorrido de MGenética"}
**Cierre del sendero**

Después de este módulo, revise la interpretación que escribió en los puntos de control anteriores. El certificado es el cierre formal, pero la evidencia del aprendizaje es la capacidad de explicar cuándo utilizar pedigrí, matriz genómica, GWAS o predicción para una decisión de selección.
:::

::: {.module-close-check role="region" aria-label="Revisión de cierre del módulo"}
::: {.module-close-check-copy}
**Antes de cambiar de página**

Cierre el módulo cuando la lectura, el código y el cuestionario apoyen la misma conclusión. Si queda algún punto abierto, retroceda en el índice o busque antes de seguir adelante.
:::

::: {.module-close-check-grid role="list" aria-label="Criterios de cierre del módulo"}
::: {.module-close-check-item role="listitem"}
**Lectura**

La pregunta central fue respondida con sus palabras.
:::

::: {.module-close-check-item role="listitem"}
**Código**

El resultado principal fue interpretado, no solo ejecutado.
:::

::: {.module-close-check-item role="listitem"}
**Prueba**

El resultado confirma la disposición a avanzar o revisar.
:::
:::
:::

::: {.module-return-note role="note" aria-label="Cómo volver al flujo después de este módulo"}
**Vuelve con una decisión**

Antes de cambiar de página, registre si este módulo generó avance, revisión o consulta. Utilice esta decisión para elegir el siguiente clic en el índice.
:::

```{=html}
<nav class="module-nav" aria-label="Navegación entre módulos">
  <a class="module-nav-card module-nav-index" href="index.html">
    <span>Índice</span>
    <strong>Todos los módulos</strong>
  </a>
  <a class="module-nav-card module-nav-prev" href="modulo11-controle-de-qualidade-de-dados-genomicos.html">
    <span>Anterior · Módulo 11</span>
    <strong>Control de calidad genómica</strong>
  </a>
  <a class="module-nav-card module-nav-next" href="../certificado.html">
    <span>Conclusión</span>
    <strong>Generar certificado</strong>
  </a>
</nav>
```
