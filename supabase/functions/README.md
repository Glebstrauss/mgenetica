Deprecated scaffold

This directory is legacy Supabase experiment material.

- Active backend path for the learner app is Appwrite, not Supabase.
- Active deploy workflows are:
  - `.github/workflows/deploy-frontend.yml`
  - `.github/workflows/appwrite-deploy.yml`

Do not wire new production work to this Supabase scaffold unless the backend direction is explicitly changed.
